"""
Exercicios:

1) Faça cada passo da tartaruga ter uma cor diferente
2) Aumente/diminua o diâmetro do círculo
"""

import turtle
import random

turtle = turtle.Turtle()

colors = ['red', 'blue', 'orange', 'purple', 'yellow', 'green', 'brown', 'pink']

for c in range(180):
    turtle.color(random.choice(colors))
    turtle.forward(1)
    turtle.right(2)
